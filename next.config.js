/** @type {import('next').NextConfig} */
const nextConfig = {
  // Puedes añadir aquí configuraciones futuras si las necesitas.
  // Por ahora, lo dejamos vacío si no hay configuraciones específicas.
};

module.exports = nextConfig;